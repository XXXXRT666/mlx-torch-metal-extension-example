[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mlx-torch-metal-extension-example"
version = "0.1.0"
requires-python = ">=3.10"
dynamic = ["dependencies"]

